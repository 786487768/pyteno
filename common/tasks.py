import os
import uuid

class Task(object):
    def __init__(self, task_exec, task_args, job_id, input_file, output_file, ):
       input_file =


if __name__ == '__main__':
    new_task = Task()
    print(new_task.name)
